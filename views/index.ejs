<%- include('header') -%>
<div class="cards">
<% filterFM.forEach((item) => { %>
    <a href="./<%= item.slug %>">
        <div class="card">
            <div clas="card__image">
                <% if(item.image){ %> <img src="<%= item.image %>" alt=""><% }else{ %> <span class="card__fill"></span> <% } %>
            </div>
            <div class="card__content">
                <h2><%= item.title %></h2>
                <p><%= moment(item.date).format( 'DD.MM.YYYY') %></p>
            </div>
        </div>
    </a>
  <% }) %>
</div>
  
<%- include('footer') -%>